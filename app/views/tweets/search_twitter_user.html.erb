<%= form_tag tweets_by_user_path, :method => 'get' do %>
  <label>Search for User by handle</label>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>
